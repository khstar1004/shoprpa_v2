<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.iflytek.rpa.auth.sp.casdoor.dao.CasdoorTenantDao">

    <!-- 가져오기지원하지 않는유형의테넌트ID목록 -->
    <select id="getNoClassifyTenantIds" resultType="java.lang.String">
        select
            name
        from ${databaseName}.`organization`
        where
            name not in ('built-in')
    </select>

</mapper>
