확장 프로그램 설치에 실패했습니다.
1. Windows에서는 `windows_policy.reg` 파일을 실행하세요.
2. Linux에서는 `copy_policy.sh`를 실행하세요. sudo 권한이 필요할 수 있습니다. 또는 `policy.json`을 `/etc/opt/chrome/policies/managed` 디렉터리에 복사하세요. 해당 디렉터리가 없으면 먼저 생성해야 합니다.
